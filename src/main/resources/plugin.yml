name: ${project.name}
author: DreamVoid
description: ${project.description}
main: me.dreamvoid.commandpermissionmanager.main
version: ${project.version}
api-version: 1.13
prefix: DreamVoidPlugin
webside: ${project.url}
load: POSTWORLD
commands:
  commandpermissionmanager:
    description: CommandPermissionManager main command.
    permission: commandpermissionmanager.command
    aliases:
      - cpm
permissions:
  commandpermissionmanager.command:
    description: Allow use /commandpermissionmanager
    default: op
